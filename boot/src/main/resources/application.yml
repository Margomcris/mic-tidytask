server:
  port: 8081
  servlet:
    context-path: /tidytask
spring:
  application:
    name: mic-tidytask
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
